import React from 'react';
import {View, Text} from 'react-native';
import ExpensePrimaryScreen from './PrimaryScreen';

const ExpenseScreen = props => {
  return <ExpensePrimaryScreen />;
};

export default ExpenseScreen;
