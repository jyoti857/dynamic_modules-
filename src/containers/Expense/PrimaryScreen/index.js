import React from 'react';
import ExpenseDashboard from './components/ExpenseDashboard';

const ExpensePrimaryScreen = props => {
  return <ExpenseDashboard />;
};

export default ExpensePrimaryScreen;
