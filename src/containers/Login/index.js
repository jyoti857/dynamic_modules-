import React from 'react';
import {TextInput} from 'react-native-paper';
import {View, Text} from 'react-native';

const Login = props => {
  return (
    <View>
      <TextInput
        label="email"
        // value = {}
        onChangeText={() => {}}
      />
    </View>
  );
};
